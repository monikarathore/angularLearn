import { EmployeDirective } from './employe.directive';

describe('EmployeDirective', () => {
  it('should create an instance', () => {
    const directive = new EmployeDirective();
    expect(directive).toBeTruthy();
  });
});
